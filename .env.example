# Variables de entorno para el backend
# Base env (copia a .env.local / .env.dev / .env.prod según necesites)
ENV=local
MONGO_URI=mongodb://mongo:27017/mlog
LOG_LEVEL=debug
PORT=8000

# CORS (opcional, para frontend local)
CORS_ORIGINS=http://localhost:5173,http://127.0.0.1:5173,http://localhost:8080

# Auth (opcional)
# Comma-separated API keys. Si está vacío y no hay JWT_SECRET, auth queda deshabilitada.
API_KEYS=
# Si prefieres JWT, define el secreto HS256
JWT_SECRET=

# Rate limiting por minuto
RATE_LIMIT_PER_MIN=60

# Redis (caché analytics)
REDIS_URL=redis://redis:6379/0
ANALYTICS_CACHE_TTL=30

# OpenTelemetry (opcional)
# Endpoint del colector OTLP (ej: http://otel-collector:4318/v1/traces o host:4317 para gRPC)
OTEL_EXPORTER_OTLP_ENDPOINT=
