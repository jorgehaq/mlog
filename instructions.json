## Seguridad

- **Authentication**: Sin autenticación en endpoints, implementar JWT/API keys
- **CORS**: `allow_origins=["*"]` en producción, restringir dominios
- **Input validation**: Sin sanitización de metadata JSON
- **Rate limiting**: Sin throttling para prevenir abuse
